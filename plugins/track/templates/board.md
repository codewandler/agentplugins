# {{PROJECT}} — backlog & status board

The single screen for **"what to work on next and where we are."** One file per story lives in this
directory (`<ID>-<slug>.md`; frontmatter carries `status`/`priority`/`pillar`/`epic`). The lists below
are **generated** from that frontmatter by `/track:board` — edit the stories, not the generated region.
New work? Copy [`_TEMPLATE.md`](_TEMPLATE.md) or run `/track:story`. For the bigger picture see the
[roadmap](../roadmap.md); for the working loop see [AGENTS.md](../../AGENTS.md) → **"Start here"**.

## Status
- _Summarize the current release / what's in flight / the gate state here. This section is
  hand-written and preserved across board regenerations._

<!-- BEGIN track:board -->
<!-- (run /track:board to populate) -->
<!-- END track:board -->
